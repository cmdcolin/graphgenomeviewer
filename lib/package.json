{
  "name": "graphgenomeviewer",
  "version": "2.0.2",
  "description": "React component for graph visualization",
  "author": "cmdcolin",
  "license": "MIT",
  "repository": "cmdcolin/graphgenomeviewer",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "build": "rm -rf dist && babel src --out-dir dist",
    "watch": "babel src --out-dir dist --watch",
    "postversion": "git push --follow-tags"
  },
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.14",
    "@babel/preset-react": "^7.13.13"
  },
  "dependencies": {
    "d3": "^6.6.2",
    "mathjs": "^9.3.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
